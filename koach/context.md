# קרא וחתום (קו"ח) — Developer Context

> קובץ תיעוד בין-שיחות (מתכנת ← מתכנת), בעברית RTL.
> **מצב נוכחי: שלב תכנון בלבד — הווידג'ט עדיין לא נבנה.**
> עודכן לאחרונה: יולי 2026 (שיחה 1 — תכנון)

---

## 1. מטרה

ווידג'ט **"קרא וחתום" (קו"ח)** לתוך דשבורד הטייסים של היחידה האווירית (משטרת ישראל).
כשטייס מגיע למשמרת ופותח את הדשבורד, הוא רואה את רשימת הקו"חים האחרונים שהמפקד פרסם,
קורא אותם ומאשר קריאה. המערכת עוקבת מי קרא ומי לא — לצורך "שיימינג" חיובי שגורם לכולם לקרוא.

הווידג'ט משולב בדשבורד באמצעות **iframe** (החבר קרא לזה "INFRAME") — כלומר הוא אפליקציה
עצמאית שמתארחת בכתובת משלה, וה-HTML של הדשבורד מטמיע אותה עם `<iframe src="...">`.

---

## 2. דרישות מוצר (מהלקוח)

| # | דרישה |
|---|-------|
| 1 | רק **המפקד** יכול להעלות קו"ח (מסמך Word / PDF / או טקסט חופשי שמוצג יפה ויזואלית) |
| 2 | כל הטייסים צריכים לקרוא את הקו"ח שהועלה |
| 3 | לכל טייס רשימת הקו"חים **האחרונים** שהמפקד העלה |
| 4 | קו"ח שקראתי → מסומן **ירוק**. קו"ח שלא קראתי → מסומן **אדום** |
| 5 | **Hover** על קו"ח → חלון קופץ (tooltip/popover) עם רשימת מי קרא ומי לא קרא. כל טייס רואה את כולם (זה ה"שיימינג") |
| 6 | תמיד מוצגים לפחות **5 הקו"חים האחרונים** + **כל קו"ח שהטייס הנוכחי עדיין לא קרא** (גם ישן יותר) |
| 7 | כפתור **רענון** |
| 8 | תצוגה ויזואלית יפה, עברית RTL |
| 9 | הטמעה בדשבורד באמצעות **iframe** |

---

## 3. אתגר ארכיטקטוני מרכזי — צריך Backend

האפליקציות הקיימות ברפו (`h125wb`, `h145wb`) הן **סטטיות לחלוטין** (GitHub Pages, ללא שרת).
קרא וחתום **לא יכול** להיות סטטי, כי הוא דורש **מצב משותף ומתמשך בין משתמשים**:
- אחסון הקו"חים שהמפקד מעלה (טקסט + קבצים)
- מעקב "מי קרא מה" — משותף לכל הטייסים, מסונכרן
- זהות המשתמש (מי אני, האם אני מפקד)

לכן צריך שכבת backend + אחסון קבצים + זהות.

### המלצה: **Firebase** (חבילת Google)
- **Firestore** — DB בזמן אמת לרשימת הקו"חים + מטריצת "מי קרא מה"
- **Firebase Storage** — אחסון קבצי ה-PDF/Word
- **Firebase Auth** — זהות טייס (Google / Email) + הרשאת מפקד
- שכבה חינמית נדיבה, סנכרון בזמן אמת (חשוב לסימון ירוק/אדום שמתעדכן חי), אין להחזיק שרת
- חלופה: **Supabase** (Postgres + Auth + Storage) — אם מעדיפים SQL

> ⚠️ **הערת אבטחה (הקשר משטרתי):** מסמכים רגישים. לפני production צריך להחליט על ריבונות מידע,
> הרשאות, ואולי אחסון בתוך רשת היחידה במקום ענן ציבורי. לפרוטוטייפ — Firebase מספיק.

### החלטות פתוחות (לתחילת שיחה 2)
- [ ] **מיקום ריפו:** תת-תיקייה חדשה ברפו הקיים (`koach/` — מומלץ, בטוח, מבודד כמו `h145wb`) **או** ריפו חדש נפרד
- [ ] **Backend:** Firebase (מומלץ) / Supabase / אחר
- [ ] **זהות + Auth:** איך טייס מזוהה? (Firebase Auth / רשימת טייסים קבועה + login / זהות מועברת מהדשבורד ההורה דרך postMessage)
- [ ] **רשימת הטייסים המלאה** — נדרשת כדי להציג "אדומים" (מי שלא קרא). מאיפה מגיעה?
- [ ] **מי המפקד** — איך מסמנים הרשאת admin?

---

## 4. מחסנית טכנית מוצעת

| נושא | בחירה | הערה |
|------|-------|------|
| Framework | React 19 + TypeScript | תואם לאפליקציות הקיימות |
| Build | Vite 8 + Tailwind v4 | זהה ל-`h125wb` |
| Backend | Firebase (Firestore + Storage + Auth) | *טעון אישור* |
| הצגת PDF | `pdf.js` / `<embed>` native | |
| הצגת Word (.docx) | `mammoth.js` (docx→HTML בצד לקוח) | Word לא נפתח native בדפדפן |
| הצגת טקסט חופשי | עורך פשוט (Markdown / rich text) | |
| הטמעה | iframe + `postMessage` לגובה דינמי | |
| Deploy | GitHub Pages (כמו הקיים) **או** Firebase Hosting | |

---

## 5. מודל נתונים מוצע (Firestore)

```
koachim (collection)               // כל מסמכי הקו"ח
  {koachId}
    title: string
    type: "pdf" | "docx" | "text"
    body: string                   // אם text — התוכן; אם קובץ — URL ל-Storage
    fileUrl: string | null
    createdBy: uid                 // המפקד
    createdAt: timestamp
    reads (subcollection)          // מי קרא
      {uid}
        readAt: timestamp

pilots (collection)                // רוסטר הטייסים (לצורך הצגת מי לא קרא)
  {uid}
    name: string
    role: "pilot" | "commander"
```

חוקי הצגה (דרישה 6): `latest 5 by createdAt` ∪ `{כל קו"ח שאין בו read למשתמש הנוכחי}`.

---

## 6. תכנית עבודה — שלבים

### Phase 0 — תכנון (✅ השיחה הזו)
- [x] הבנת מבנה הרפו הקיים והבטחה שלא נהרס
- [x] זיהוי הצורך ב-backend
- [x] כתיבת `koach/context.md` (הקובץ הזה)
- [ ] קבלת החלטות פתוחות מהלקוח (סעיף 3)

### Phase 1 — תשתית
- [ ] יצירת אפליקציית Vite+React+TS בתיקייה `koach/` (מבודדת, לא נוגעת ב-h125wb/h145wb)
- [ ] הקמת פרויקט Firebase + הגדרת Firestore/Storage/Auth
- [ ] חיבור בסיסי + משתני סביבה (Firebase config)

### Phase 2 — צד המפקד (upload)
- [ ] מסך/טופס העלאה — טקסט חופשי + העלאת PDF/Word
- [ ] הגבלת גישה: רק role=commander
- [ ] שמירה ב-Firestore + Storage

### Phase 3 — צד הטייס (רשימה + קריאה)
- [ ] רשימת קו"חים לפי חוק ההצגה (5 אחרונים + כל מה שלא נקרא)
- [ ] סימון ירוק/אדום לפי סטטוס קריאה של המשתמש הנוכחי
- [ ] פתיחת קו"ח → הצגת PDF/docx/טקסט יפה
- [ ] כפתור "קראתי / אשר" → כתיבת read ל-Firestore
- [ ] כפתור רענון

### Phase 4 — שיימינג (tooltip מי קרא/לא קרא)
- [ ] Hover על קו"ח → popover עם רשימת כל הטייסים: ירוק=קרא, אדום=לא
- [ ] נראות לכל הטייסים

### Phase 5 — הטמעה כ-iframe ("INFRAME")
- [ ] בניית build עצמאי
- [ ] טיפול בגובה דינמי דרך `postMessage`
- [ ] העברת זהות הטייס מהדשבורד ההורה (אם רלוונטי)
- [ ] קטע `<iframe>` מוכן להדבקה בדשבורד

### Phase 6 — עיצוב + ליטוש
- [ ] עיצוב RTL יפה, תואם לשפת העיצוב של הדשבורד
- [ ] מצבי טעינה/שגיאה/ריק
- [ ] בדיקות

### Phase 7 — Deploy
- [ ] הוספת ה-build ל-workflow (`.github/workflows/deploy.yml`) — נתיב נפרד `/koach/`, בלי לגעת בקיים
- [ ] או Firebase Hosting נפרד

---

## 7. הבטחת אי-הרס לאפליקציות הקיימות

- כל הקוד החדש ב-`koach/` בלבד — תיקייה נפרדת, בדיוק כמו ש-`h145wb/` לא נוגע ב-`h125wb/`
- ה-`deploy.yml` הקיים לא ישונה עד Phase 7, ואז רק **הוספה** של step (לא עריכת steps קיימים)
- אין שינוי בשום קובץ של h125wb/h145wb

---

## 8. Git

| נושא | ערך |
|------|-----|
| Repo | `arnoni-oss/h125-weight-balance` |
| ענף עבודה | `claude/police-dashboard-read-sign-31cz2u` |
| ענף deploy קיים (לא לגעת עד Phase 7) | `claude/happy-volta-BBzTA` |

---

## 9. יומן שיחות

### שיחה 1 (יולי 2026) — תכנון
- מיפוי הרפו הקיים (מונו-רפו, 2 אפליקציות סטטיות)
- זיהוי שקו"ח דורש backend (שינוי ארכיטקטוני מרכזי)
- כתיבת קובץ context זה + תכנית עבודה 7 שלבים
- **לא נבנה קוד עדיין** (לבקשת הלקוח)
- ממתין להחלטות: מיקום ריפו, backend, זהות/auth, רוסטר טייסים
