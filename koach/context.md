# קרא וחתום (קו"ח) — Developer Context

> קובץ תיעוד בין-שיחות (מתכנת ← מתכנת), בעברית RTL.
> **מצב נוכחי: שלב תכנון בלבד — הווידג'ט עדיין לא נבנה.**
> עודכן לאחרונה: יולי 2026 (שיחה 1 — תכנון)

---

## 1. מטרה

ווידג'ט **"קרא וחתום" (קו"ח)** לתוך דשבורד הטייסים של היחידה האווירית (משטרת ישראל).
כשטייס מגיע למשמרת ופותח את הדשבורד, הוא רואה את רשימת הקו"חים האחרונים שהמפקד פרסם,
קורא אותם ומאשר קריאה. המערכת עוקבת מי קרא ומי לא — לצורך "שיימינג" חיובי שגורם לכולם לקרוא.

הווידג'ט משולב בדשבורד באמצעות **iframe** (החבר קרא לזה "INFRAME") — כלומר הוא אפליקציה
עצמאית שמתארחת בכתובת משלה, וה-HTML של הדשבורד מטמיע אותה עם `<iframe src="...">`.

---

## 2. דרישות מוצר (מהלקוח)

| # | דרישה |
|---|-------|
| 1 | רק **המפקד** יכול להעלות קו"ח (מסמך Word / PDF / או טקסט חופשי שמוצג יפה ויזואלית) |
| 2 | כל הטייסים צריכים לקרוא את הקו"ח שהועלה |
| 3 | לכל טייס רשימת הקו"חים **האחרונים** שהמפקד העלה |
| 4 | קו"ח שקראתי → מסומן **ירוק**. קו"ח שלא קראתי → מסומן **אדום** |
| 5 | **Hover** על קו"ח → חלון קופץ (tooltip/popover) עם רשימת מי קרא ומי לא קרא. כל טייס רואה את כולם (זה ה"שיימינג") |
| 6 | תמיד מוצגים לפחות **5 הקו"חים האחרונים** + **כל קו"ח שהטייס הנוכחי עדיין לא קרא** (גם ישן יותר) |
| 7 | כפתור **רענון** |
| 8 | תצוגה ויזואלית יפה, עברית RTL |
| 9 | הטמעה בדשבורד באמצעות **iframe** |

---

## 3. אתגר ארכיטקטוני מרכזי — צריך Backend

האפליקציות הקיימות ברפו (`h125wb`, `h145wb`) הן **סטטיות לחלוטין** (GitHub Pages, ללא שרת).
קרא וחתום **לא יכול** להיות סטטי, כי הוא דורש **מצב משותף ומתמשך בין משתמשים**:
- אחסון הקו"חים שהמפקד מעלה (טקסט + קבצים)
- מעקב "מי קרא מה" — משותף לכל הטייסים, מסונכרן
- זהות המשתמש (מי אני, האם אני מפקד)

לכן צריך שכבת backend + אחסון קבצים + זהות.

### המלצה: **Firebase** (חבילת Google)
- **Firestore** — DB בזמן אמת לרשימת הקו"חים + מטריצת "מי קרא מה"
- **Firebase Storage** — אחסון קבצי ה-PDF/Word
- **Firebase Auth** — זהות טייס (Google / Email) + הרשאת מפקד
- שכבה חינמית נדיבה, סנכרון בזמן אמת (חשוב לסימון ירוק/אדום שמתעדכן חי), אין להחזיק שרת
- חלופה: **Supabase** (Postgres + Auth + Storage) — אם מעדיפים SQL

> ⚠️ **הערת אבטחה (הקשר משטרתי):** מסמכים רגישים. לפני production צריך להחליט על ריבונות מידע,
> הרשאות, ואולי אחסון בתוך רשת היחידה במקום ענן ציבורי. לפרוטוטייפ — Firebase מספיק.

### החלטות שהתקבלו (שיחה 1)
- [x] **מיקום ריפו:** ✅ **ריפו GitHub חדש ונפרד** (לא בתוך weight-balance). ליצור כשמתחילים לבנות.
- [x] **זהות:** ✅ **מגיעה מהדשבורד ההורה** — לא Auth עצמאי בווידג'ט.
- [ ] **Backend:** פתוח. הלקוח ביקש לדון באבטחה קודם (הקשר משטרתי, מסמכים רגישים).

### נגזרות אבטחה מההחלטות (חשוב!)
1. **אמון בזהות = קריטי.** הדשבורד חייב להעביר **JWT חתום** (עדיף דרך `postMessage`, לא ב-URL), וה-backend
   חייב **לאמת את החתימה** לפני רישום "קראתי". אחרת אפשר לזייף חתימת קריאה. חתימת קו"ח = בלתי ניתנת להכחשה.
2. **עדיף שה-backend של הדשבורד יחזיק את המידע.** אם למערכת האב כבר יש backend/API — הקו"חים ומטריצת
   "מי קרא" צריכים לשבת שם (אותו גבול אמון, אותה ריבונות), במקום דאטהבייס ענני נפרד עם עותק שני של מסמכים רגישים.
3. **שיימינג = חשיפה מכוונת בתוך קבוצת הטייסים** (by-design, בסדר). הרגישות היא בתוכן המסמכים ובגישה מאומתת בלבד.

### אפשרויות Backend (לפי ריבונות מידע)
| אפשרות | ריבונות | מאמץ | מתי |
|--------|---------|------|-----|
| **A. Backend קיים של הדשבורד** | הכי טוב | תלוי ב-API | production ⭐ |
| **B. שרת עצמאי ברשת היחידה** (Node+DB / Supabase self-hosted) | טוב | בינוני-גבוה | production בלי backend קיים |
| **C. ענן מנוהל** (Firebase/Supabase, EU) | חלש | נמוך | פרוטוטייפ / לא מסווג בלבד |

### 🔴 שאלה חוסמת פתוחה (לתחילת שיחה 2)
- [ ] **מה התשתית של הדשבורד ההורה?** האם יש לו backend/API שאפשר לחבר אליו (אפשרות A), ובאיזו טכנולוגיה?
      זה קובע את בחירת ה-backend, מודל האימות (JWT), ואיפה יושבים המסמכים.
- [ ] **מי המפקד** — איך מסומנת הרשאת admin (role בטוקן מהדשבורד?)
- [ ] **רשימת הטייסים המלאה** — מאיפה (מהדשבורד?) — נדרשת להצגת ה"אדומים".

---

## 4. מחסנית טכנית — נקבעה (שיחה 2)

**תשתית הדשבורד ההורה:** PHP 5.4 + MySQLi. Login: TBD (מסך login / GET / SSO בהמשך).
→ קרא וחתום נבנה כחלק מאותה מערכת: PHP endpoints על אותו שרת, אותה DB.

| שכבה | בחירה | הערה |
|------|-------|------|
| Backend | **PHP 5.4 + MySQLi** | אותו שרת ו-DB של הדשבורד. prepared statements תמיד |
| זהות/Auth | **`$_SESSION` משותף (same-origin iframe)** | ראה סעיף 4.1 — עדיף על GET/JWT |
| Frontend | **Vanilla JS + HTML/CSS** (ללא build step) | קל לתחזוקה לצוות PHP; אפשר Alpine.js קליל אם צריך |
| הצגת PDF | `<embed>` / `pdf.js` | |
| הצגת Word (.docx) | `mammoth.js` (docx→HTML בצד לקוח) | Word לא נפתח native בדפדפן |
| הצגת טקסט חופשי | Markdown / rich text מוצג יפה | |
| אחסון קבצים | תיקייה בשרת (`uploads/koach/`), מוגנת גישה | לא לחשוף directory listing |
| עדכון ירוק/אדום | polling תקופתי + כפתור רענון | בלי websockets — פשוט מספיק |
| הטמעה | iframe same-origin + `postMessage` לגובה דינמי | |
| Deploy | על שרת ה-PHP של הדשבורד, נתיב `/koach/` | הריפו ב-GitHub נפרד; פריסה לשרת |

### 4.1 מודל זהות — Session משותף (החלטה מרכזית)
הווידג'ט מוגש **מאותו domain** של הדשבורד. לכן:
1. הטייס מתחבר לדשבורד → PHP יוצר `$_SESSION`.
2. ה-iframe נטען מאותו origin → הדפדפן שולח אוטומטית את אותו session cookie.
3. קוד הקו"ח קורא `$_SESSION['user_id']` → מזהה בוודאות מי הטייס, **בלי אפשרות זיוף**.

⚠️ **דרישת אינטגרציה:** ה-iframe חייב להיות same-origin (אותו domain, או subdomain עם `SameSite`/cookie מתאים),
אחרת דפדפנים חוסמים third-party cookies והזהות תישבר. זהות דרך פרמטר GET = **פסולה** (ניתנת לזיוף).
אם בעתיד SSO — הוא יזהה מול הדשבורד, וה-session ימשיך לעבוד אותו דבר.

> ⚠️ **סיכון: PHP 5.4 = EOL מ-2015** (אין עדכוני אבטחה ~10 שנים). לא חוסם (כותבים קוד תואם 5.4 + prepared
> statements), אבל סיכון אבטחה אמיתי למערכת משטרתית — לשקול שדרוג PHP. שים לב: `password_hash()` לא קיים ב-5.4
> (נוסף ב-5.5) — אך ה-login ממילא באחריות הדשבורד, לא הווידג'ט.

---

## 5. מודל נתונים — MySQL (MySQLi)

```sql
-- טבלת קו"חים
CREATE TABLE koach (
  id          INT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  title       VARCHAR(255) NOT NULL,
  type        ENUM('pdf','docx','text') NOT NULL,
  body        MEDIUMTEXT NULL,          -- אם type=text: התוכן. אחרת NULL
  file_path   VARCHAR(500) NULL,        -- אם קובץ: נתיב יחסי ב-uploads/koach/
  created_by  INT UNSIGNED NOT NULL,    -- user_id של המפקד (מטבלת המשתמשים של הדשבורד)
  created_at  DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP
);

-- מי קרא מה (חתימת קריאה)
CREATE TABLE koach_reads (
  koach_id  INT UNSIGNED NOT NULL,
  user_id   INT UNSIGNED NOT NULL,      -- מ-$_SESSION, לא ניתן לזיוף
  read_at   DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (koach_id, user_id),
  FOREIGN KEY (koach_id) REFERENCES koach(id) ON DELETE CASCADE
);
```

- **רוסטר הטייסים + מי המפקד** — מגיעים מטבלת המשתמשים **הקיימת** של הדשבורד (לברר שם טבלה + שדה role/rank).
  לא יוצרים טבלת משתמשים כפולה. מפקד = role/דגל בטבלה הקיימת.
- **חוק הצגה (דרישה 6):** `5 האחרונים לפי created_at` ∪ `{כל קו"ח שאין לו שורת read ל-user_id הנוכחי}`.

### Endpoints מתוכננים (PHP, כולם בודקים `$_SESSION`)
| Endpoint | מי | פעולה |
|----------|-----|-------|
| `GET  list.php` | כל טייס | מחזיר את רשימת הקו"חים לפי חוק ההצגה + סטטוס קריאה שלי |
| `GET  view.php?id=` | כל טייס | מחזיר תוכן/קובץ קו"ח (בדיקת session לפני הגשת קובץ) |
| `POST read.php` | כל טייס | רושם `koach_reads` ל-user_id מה-session |
| `GET  readers.php?id=` | כל טייס | מי קרא / מי לא (ל-tooltip השיימינג) |
| `POST upload.php` | **מפקד בלבד** | יוצר קו"ח (טקסט/קובץ). בודק role=commander |

---

## 6. תכנית עבודה — שלבים

### Phase 0 — תכנון (✅ השיחה הזו)
- [x] הבנת מבנה הרפו הקיים והבטחה שלא נהרס
- [x] זיהוי הצורך ב-backend
- [x] כתיבת `koach/context.md` (הקובץ הזה)
- [ ] קבלת החלטות פתוחות מהלקוח (סעיף 3)

### Phase 1 — תשתית
- [ ] יצירת ריפו GitHub חדש ונפרד לקו"ח
- [ ] הרצת ה-SQL (טבלאות `koach`, `koach_reads`) על ה-DB של הדשבורד
- [ ] קובץ `db.php` להתחברות MySQLi + `auth.php` שקורא `$_SESSION` ומחזיר user_id/role
- [ ] לברר מול צוות הדשבורד: שם טבלת המשתמשים, שדה role/מפקד, מפתח session

### Phase 2 — צד המפקד (upload)
- [ ] מסך/טופס העלאה — טקסט חופשי + העלאת PDF/Word
- [ ] הגבלת גישה: רק role=commander
- [ ] שמירה ב-Firestore + Storage

### Phase 3 — צד הטייס (רשימה + קריאה)
- [ ] רשימת קו"חים לפי חוק ההצגה (5 אחרונים + כל מה שלא נקרא)
- [ ] סימון ירוק/אדום לפי סטטוס קריאה של המשתמש הנוכחי
- [ ] פתיחת קו"ח → הצגת PDF/docx/טקסט יפה
- [ ] כפתור "קראתי / אשר" → כתיבת read ל-Firestore
- [ ] כפתור רענון

### Phase 4 — שיימינג (tooltip מי קרא/לא קרא)
- [ ] Hover על קו"ח → popover עם רשימת כל הטייסים: ירוק=קרא, אדום=לא
- [ ] נראות לכל הטייסים

### Phase 5 — הטמעה כ-iframe ("INFRAME")
- [ ] בניית build עצמאי
- [ ] טיפול בגובה דינמי דרך `postMessage`
- [ ] העברת זהות הטייס מהדשבורד ההורה (אם רלוונטי)
- [ ] קטע `<iframe>` מוכן להדבקה בדשבורד

### Phase 6 — עיצוב + ליטוש
- [ ] עיצוב RTL יפה, תואם לשפת העיצוב של הדשבורד
- [ ] מצבי טעינה/שגיאה/ריק
- [ ] בדיקות

### Phase 7 — Deploy
- [ ] הוספת ה-build ל-workflow (`.github/workflows/deploy.yml`) — נתיב נפרד `/koach/`, בלי לגעת בקיים
- [ ] או Firebase Hosting נפרד

---

## 7. הבטחת אי-הרס לאפליקציות הקיימות

- כל הקוד החדש ב-`koach/` בלבד — תיקייה נפרדת, בדיוק כמו ש-`h145wb/` לא נוגע ב-`h125wb/`
- ה-`deploy.yml` הקיים לא ישונה עד Phase 7, ואז רק **הוספה** של step (לא עריכת steps קיימים)
- אין שינוי בשום קובץ של h125wb/h145wb

---

## 8. Git

| נושא | ערך |
|------|-----|
| Repo | `arnoni-oss/h125-weight-balance` |
| ענף עבודה | `claude/police-dashboard-read-sign-31cz2u` |
| ענף deploy קיים (לא לגעת עד Phase 7) | `claude/happy-volta-BBzTA` |

---

## 9. יומן שיחות

### שיחה 1 (יולי 2026) — תכנון
- מיפוי הרפו הקיים (מונו-רפו, 2 אפליקציות סטטיות)
- זיהוי שקו"ח דורש backend (שינוי ארכיטקטוני מרכזי)
- כתיבת קובץ context זה + תכנית עבודה 7 שלבים
- **לא נבנה קוד עדיין** (לבקשת הלקוח)
- **החלטות שהתקבלו:** ריפו חדש נפרד ✅ · זהות מהדשבורד ההורה ✅ · backend פתוח (דיון אבטחה)
- **דיון אבטחה:** צורך ב-JWT חתום מהדשבורד; עדיפות שה-backend של הדשבורד יחזיק את המידע; ריבונות מידע

### שיחה 2 (יולי 2026) — קביעת ארכיטקטורה
- **תשתית הדשבורד נחשפה:** PHP 5.4 + MySQLi, login TBD (מסך login/GET/SSO בהמשך)
- **הוכרע:** Backend = PHP+MySQLi על אותו שרת · Frontend = Vanilla JS (בלי build) · הטמעה = iframe same-origin
- **זהות = `$_SESSION` משותף** (same-origin) — פשוט ומאובטח יותר מ-JWT/GET; GET נפסל (זיוף)
- עודכנו: מחסנית (סעיף 4+4.1), מודל נתונים MySQL + endpoints (סעיף 5), Phase 1
- **סיכון שנרשם:** PHP 5.4 = EOL 2015
- **לברר מול צוות הדשבורד (לתחילת שיחה 3):** שם domain (לוודא same-origin ל-iframe) · שם טבלת המשתמשים
  ושדה role/מפקד · מפתח ה-session (`$_SESSION['?']`) · היכן uploads/ · האם login כבר קיים או שנחכה ל-SSO
- **עדיין לא נבנה קוד** (לבקשת הלקוח — תכנון בלבד)
